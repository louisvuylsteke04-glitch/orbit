// Orbit configuration — TEMPLATE.
// Copy this file to `config.js` and fill in your own Supabase project values.
// `config.js` is gitignored, so your credentials never get committed.
//
// Where to find these: Supabase dashboard → your project → Settings → API
//   - Project URL                  → SUPABASE_URL
//   - Publishable key (or anon key) → SUPABASE_KEY   (safe to expose in the client)
//
// See SUPABASE_SETUP.md for the full "bring your own Supabase" walkthrough.
window.ORBIT_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT-REF.supabase.co",
  SUPABASE_KEY: "YOUR-PUBLISHABLE-OR-ANON-KEY",
};
